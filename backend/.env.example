# Server
PORT=5000
NODE_ENV=development

# Neon PostgreSQL
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require

# JWT
JWT_SECRET=cambia_esto_en_produccion
JWT_EXPIRES_IN=24h

# Superadmin (no puede perder el rol admin por otros administradores)
SUPER_ADMIN_EMAIL=fernandoalexisflorarganaraz@gmail.com

# Frontend URL (CORS) — separar múltiples orígenes con coma
FRONTEND_URL=http://localhost:5173

# Email (reset password + newsletter) — ej. Gmail, Brevo, SendGrid SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu_app_password
EMAIL_FROM=CrumbSkate <noreply@crumbskate.com>
